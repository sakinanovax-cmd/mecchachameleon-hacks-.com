/**
 * Site analytics / Search Console config.
 * Fill these after deploy, then leave this file as-is.
 * Empty strings = scripts stay disabled (no broken IDs).
 */
window.SITE_CONFIG = {
  // Google Analytics 4 Measurement ID, e.g. "G-XXXXXXXXXX"
  ga4Id: "",
  // Google Search Console HTML-tag verification code (content= value only)
  gscVerification: "",
  // Optional: Google Ads / conversion ID later
  adsId: ""
};
